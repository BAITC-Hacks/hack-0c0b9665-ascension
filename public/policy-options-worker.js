// Generated by scripts/build-policy-worker.mjs; edit the shared source, then rebuild.

// data/city.json
var city_default = {
  budget: 100,
  horizon: 8,
  indicators: [
    {
      id: "T1",
      name: "Разгрузка дорог",
      direction: "transport",
      weight: 0.1
    },
    {
      id: "T2",
      name: "Доступность общественного транспорта",
      direction: "transport",
      weight: 0.1
    },
    {
      id: "E1",
      name: "Озеленение",
      direction: "ecology",
      weight: 0.09
    },
    {
      id: "E2",
      name: "Качество воздуха",
      direction: "ecology",
      weight: 0.11
    },
    {
      id: "S1",
      name: "Школы и детсады",
      direction: "social",
      weight: 0.11
    },
    {
      id: "S2",
      name: "Поликлиники и первичная медпомощь",
      direction: "social",
      weight: 0.11
    },
    {
      id: "B1",
      name: "Безопасность улиц",
      direction: "safety",
      weight: 0.09
    },
    {
      id: "B2",
      name: "Безопасность дорожного движения",
      direction: "safety",
      weight: 0.09
    },
    {
      id: "C1",
      name: "Надёжность ЖКХ",
      direction: "services",
      weight: 0.1
    },
    {
      id: "C2",
      name: "Скорость решения обращений жителей",
      direction: "services",
      weight: 0.1
    }
  ],
  districts: [
    {
      id: "esil",
      name: "Есиль",
      populationShare: 0.27,
      indicators: {
        T1: 45,
        T2: 62,
        E1: 68,
        E2: 72,
        S1: 48,
        S2: 55,
        B1: 78,
        B2: 60,
        C1: 75,
        C2: 70
      }
    },
    {
      id: "almaty",
      name: "Алматы",
      populationShare: 0.24,
      indicators: {
        T1: 40,
        T2: 75,
        E1: 50,
        E2: 55,
        S1: 60,
        S2: 65,
        B1: 62,
        B2: 52,
        C1: 50,
        C2: 60
      }
    },
    {
      id: "saryarka",
      name: "Сарыарка",
      populationShare: 0.2,
      indicators: {
        T1: 50,
        T2: 70,
        E1: 42,
        E2: 40,
        S1: 62,
        S2: 68,
        B1: 58,
        B2: 55,
        C1: 45,
        C2: 55
      }
    },
    {
      id: "baikonur",
      name: "Байконур",
      populationShare: 0.13,
      indicators: {
        T1: 52,
        T2: 68,
        E1: 55,
        E2: 50,
        S1: 58,
        S2: 60,
        B1: 52,
        B2: 58,
        C1: 55,
        C2: 58
      }
    },
    {
      id: "nura",
      name: "Нура",
      populationShare: 0.16,
      indicators: {
        T1: 55,
        T2: 40,
        E1: 45,
        E2: 65,
        S1: 38,
        S2: 35,
        B1: 55,
        B2: 50,
        C1: 60,
        C2: 50
      }
    }
  ],
  measures: [
    {
      id: "M1",
      name: "Выделенные полосы для автобусов",
      direction: "transport",
      scope: "district",
      cost: 18,
      lag: 2,
      effects: {
        T1: 6,
        T2: 9
      }
    },
    {
      id: "M2",
      name: "Умные светофоры (адаптивное управление)",
      direction: "transport",
      scope: "city",
      cost: 22,
      lag: 2,
      effects: {
        T1: 4,
        B2: 3
      }
    },
    {
      id: "M3",
      name: "Линия ЛРТ / расширение",
      direction: "transport",
      scope: "district",
      cost: 30,
      lag: 4,
      effects: {
        T1: 16,
        T2: 20,
        E2: 4
      }
    },
    {
      id: "M4",
      name: "Парк / сквер",
      direction: "ecology",
      scope: "district",
      cost: 15,
      lag: 2,
      effects: {
        E1: 12,
        E2: 3,
        B1: 2
      }
    },
    {
      id: "M5",
      name: "Перевод частного сектора на чистое топливо",
      direction: "ecology",
      scope: "district",
      cost: 25,
      lag: 3,
      effects: {
        E2: 14,
        C1: 4
      }
    },
    {
      id: "M6",
      name: "Городская программа озеленения и ветрозащитных полос",
      direction: "ecology",
      scope: "city",
      cost: 20,
      lag: 4,
      effects: {
        E1: 5,
        E2: 3
      }
    },
    {
      id: "M7",
      name: "Школа + детсад (модульное строительство)",
      direction: "social",
      scope: "district",
      cost: 24,
      lag: 3,
      effects: {
        S1: 16
      }
    },
    {
      id: "M8",
      name: "Центр семейного здоровья / поликлиника",
      direction: "social",
      scope: "district",
      cost: 20,
      lag: 3,
      effects: {
        S2: 14
      }
    },
    {
      id: "M9",
      name: "Дворовые спорт-хабы",
      direction: "social",
      scope: "district",
      cost: 10,
      lag: 1,
      effects: {
        S1: 3,
        S2: 3,
        B1: 3
      }
    },
    {
      id: "M10",
      name: "Освещение и камеры (расширение Safe City)",
      direction: "safety",
      scope: "district",
      cost: 12,
      lag: 1,
      effects: {
        B1: 12,
        B2: 2
      }
    },
    {
      id: "M11",
      name: "Безопасные переходы и школьные зоны",
      direction: "safety",
      scope: "district",
      cost: 10,
      lag: 1,
      effects: {
        B2: 12,
        T1: -2
      }
    },
    {
      id: "M12",
      name: "Единая цифровая платформа обращений",
      direction: "services",
      scope: "city",
      cost: 14,
      lag: 1,
      effects: {
        C2: 5
      }
    },
    {
      id: "M13",
      name: "Модернизация тепло- и водосетей",
      direction: "services",
      scope: "district",
      cost: 28,
      lag: 4,
      effects: {
        C1: 18,
        E2: 2
      }
    },
    {
      id: "M14",
      name: "Аварийные бригады ЖКХ + раннее оповещение",
      direction: "services",
      scope: "city",
      cost: 16,
      lag: 1,
      effects: {
        C1: 5,
        C2: 2
      }
    }
  ],
  synergies: [
    {
      pair: [
        "M1",
        "M2"
      ],
      districtFrom: "M1",
      effects: {
        T1: 2
      }
    },
    {
      pair: [
        "M10",
        "M12"
      ],
      districtFrom: "M10",
      effects: {
        B1: 2
      }
    },
    {
      pair: [
        "M5",
        "M6"
      ],
      districtFrom: "M5",
      effects: {
        E2: 2
      }
    }
  ]
};

// src/core/simulator.js
var measureById = new Map(city_default.measures.map((measure) => [measure.id, measure]));
var districtIds = new Set(city_default.districts.map((district) => district.id));
var incompatibilities = [
  { pair: ["M1", "M3"], scope: "city" },
  { pair: ["M4", "M7"], scope: "district" },
  { pair: ["M5", "M13"], scope: "district" }
];
var hasOwn = (value, key) => Object.prototype.hasOwnProperty.call(value, key);
var isObject = (value) => value !== null && typeof value === "object" && !Array.isArray(value);
var clone = (value) => structuredClone(value);
function getDataset() {
  return clone(city_default);
}
function validateScenario(input) {
  const errors = [];
  let totalCost = 0;
  const error = (code, message, decisionIndex) => {
    errors.push(decisionIndex === void 0 ? { code, message } : { code, message, decisionIndex });
  };
  if (!isObject(input)) {
    error("INVALID_SCENARIO", "Сценарий должен быть объектом с массивом decisions.");
    return { valid: false, errors, totalCost };
  }
  for (const key of Object.keys(input)) {
    if (key !== "decisions") error("UNKNOWN_FIELD", "Неизвестное поле сценария: " + key + ".");
  }
  if (!Array.isArray(input.decisions)) {
    error("INVALID_DECISIONS", "Поле decisions должно быть массивом.");
    return { valid: false, errors, totalCost };
  }
  if (input.decisions.length !== 5) {
    error("DECISION_COUNT", "Нужно выбрать ровно 5 решений.");
  }
  const selected = /* @__PURE__ */ new Map();
  const directionCounts = /* @__PURE__ */ new Map();
  for (const [index, decision] of input.decisions.entries()) {
    if (!isObject(decision)) {
      error("INVALID_DECISION", "Решение должно быть объектом.", index);
      continue;
    }
    for (const key of Object.keys(decision)) {
      if (key !== "measureId" && key !== "districtId") {
        error("UNKNOWN_FIELD", "Неизвестное поле решения: " + key + ".", index);
      }
    }
    const measure = measureById.get(decision.measureId);
    if (!measure) {
      error("UNKNOWN_MEASURE", "Указано неизвестное мероприятие.", index);
      continue;
    }
    totalCost += measure.cost;
    if (selected.has(measure.id)) {
      error("DUPLICATE_MEASURE", "Мероприятие " + measure.id + " можно выбрать только один раз.", index);
    } else {
      selected.set(measure.id, { decision, index });
    }
    directionCounts.set(measure.direction, (directionCounts.get(measure.direction) ?? 0) + 1);
    if (measure.scope === "city") {
      if (hasOwn(decision, "districtId")) {
        error("CITY_DISTRICT_FORBIDDEN", "Для городской меры " + measure.id + " район не указывается.", index);
      }
    } else if (!hasOwn(decision, "districtId")) {
      error("DISTRICT_REQUIRED", "Для меры " + measure.id + " нужно выбрать район.", index);
    } else if (!districtIds.has(decision.districtId)) {
      error("UNKNOWN_DISTRICT", "Указан неизвестный район для меры " + measure.id + ".", index);
    }
  }
  for (const [direction2, count] of directionCounts) {
    if (count > 2) {
      error("DIRECTION_LIMIT", "Из направления " + direction2 + " можно выбрать не более 2 мер.");
    }
  }
  if (totalCost > city_default.budget) {
    error("BUDGET_EXCEEDED", "Стоимость " + totalCost + " превышает бюджет " + city_default.budget + ".");
  }
  for (const rule of incompatibilities) {
    const first = selected.get(rule.pair[0]);
    const second = selected.get(rule.pair[1]);
    if (!first || !second) continue;
    const sameValidDistrict = districtIds.has(first.decision.districtId) && first.decision.districtId === second.decision.districtId;
    if (rule.scope === "city" || sameValidDistrict) {
      error(
        "INCOMPATIBLE_MEASURES",
        rule.pair.join(" и ") + (rule.scope === "city" ? " несовместимы в любом районе." : " нельзя применять в одном районе."),
        second.index
      );
    }
  }
  return { valid: errors.length === 0, errors, totalCost };
}
function districtScore(indicators) {
  return city_default.indicators.reduce((sum, indicator) => sum + indicator.weight * indicators[indicator.id], 0);
}
function aggregate(districts) {
  const weightedAverage = districts.reduce((sum, district, index) => sum + city_default.districts[index].populationShare * district.afterScore, 0);
  const worstDistrictScore = Math.min(...districts.map((district) => district.afterScore));
  const criticalCount = districts.reduce((sum, district) => sum + city_default.indicators.filter((indicator) => district.after[indicator.id] < 40).length, 0);
  return {
    score: 0.7 * weightedAverage + 0.3 * worstDistrictScore - criticalCount,
    weightedAverage,
    worstDistrictScore,
    criticalCount
  };
}
var baselineDistricts = city_default.districts.map((district) => ({
  id: district.id,
  name: district.name,
  before: clone(district.indicators),
  after: clone(district.indicators),
  delta: Object.fromEntries(city_default.indicators.map((indicator) => [indicator.id, 0])),
  beforeScore: districtScore(district.indicators),
  afterScore: districtScore(district.indicators)
}));
var baseline = {
  valid: true,
  errors: [],
  totalCost: 0,
  remainingBudget: city_default.budget,
  ...aggregate(baselineDistricts),
  deltaScore: 0,
  districts: baselineDistricts,
  contributions: [],
  synergies: []
};
baseline.baselineScore = baseline.score;
function simulate(input) {
  const validation = validateScenario(input);
  if (!validation.valid) return validation;
  const decisions = [...input.decisions].sort((left, right) => Number(left.measureId.slice(1)) - Number(right.measureId.slice(1)));
  const selected = new Map(decisions.map((decision) => [decision.measureId, decision]));
  const additions = new Map(city_default.districts.map((district) => [district.id, Object.fromEntries(city_default.indicators.map((indicator) => [indicator.id, 0]))]));
  const contributions = [];
  const synergies = [];
  const addEffects = (id, effects) => {
    const target = additions.get(id);
    for (const [indicatorId, effect] of Object.entries(effects)) target[indicatorId] += effect;
  };
  for (const decision of decisions) {
    const measure = measureById.get(decision.measureId);
    const realizedFactor = (city_default.horizon - measure.lag) / city_default.horizon;
    const effects = Object.fromEntries(Object.entries(measure.effects).map(([id, effect]) => [id, effect * realizedFactor]));
    const affected = measure.scope === "city" ? city_default.districts.map((district) => district.id) : [decision.districtId];
    for (const id of affected) addEffects(id, effects);
    contributions.push({ measureId: measure.id, districtIds: affected, effects, realizedFactor });
  }
  for (const synergy of city_default.synergies) {
    if (!synergy.pair.every((id) => selected.has(id))) continue;
    const districtId = selected.get(synergy.districtFrom).districtId;
    addEffects(districtId, synergy.effects);
    synergies.push({ pair: [...synergy.pair], districtId, effects: clone(synergy.effects) });
  }
  const districts = city_default.districts.map((district) => {
    const before = clone(district.indicators);
    const after = Object.fromEntries(city_default.indicators.map(({ id }) => [id, Math.max(0, Math.min(100, before[id] + additions.get(district.id)[id]))]));
    const delta = Object.fromEntries(city_default.indicators.map(({ id }) => [id, after[id] - before[id]]));
    return {
      id: district.id,
      name: district.name,
      before,
      after,
      delta,
      beforeScore: districtScore(before),
      afterScore: districtScore(after)
    };
  });
  const metrics = aggregate(districts);
  return {
    valid: true,
    errors: [],
    totalCost: validation.totalCost,
    remainingBudget: city_default.budget - validation.totalCost,
    ...metrics,
    baselineScore: baseline.score,
    deltaScore: metrics.score - baseline.score,
    districts,
    contributions,
    synergies
  };
}

// src/core/policy-options.js
var DEFAULT_LIMIT = 6;
var MAX_LIMIT = 12;
var OBJECTIVES = ["maximizeScore", "maximizeWorst", "minimizeCost"];
var compareText = (left, right) => left < right ? -1 : left > right ? 1 : 0;
var decisionKey = ({ measureId, districtId }) => `${measureId}:${districtId ?? "*"}`;
var compareDecision = (left, right) => Number(left.measureId.slice(1)) - Number(right.measureId.slice(1)) || compareText(decisionKey(left), decisionKey(right));
var canonicalScenario = (decisions) => ({
  decisions: decisions.map((decision) => ({ ...decision })).sort(compareDecision)
});
var scenarioKey = ({ decisions }) => decisions.map(decisionKey).join("|");
function dominates(left, right) {
  return left.score >= right.score && left.worstDistrictScore >= right.worstDistrictScore && left.totalCost <= right.totalCost && (left.score > right.score || left.worstDistrictScore > right.worstDistrictScore || left.totalCost < right.totalCost);
}
function improvesAny(candidate, baseline2) {
  return candidate.score > baseline2.score || candidate.worstDistrictScore > baseline2.worstDistrictScore || candidate.totalCost < baseline2.totalCost;
}
function compareScore(left, right) {
  return right.result.score - left.result.score || right.result.worstDistrictScore - left.result.worstDistrictScore || left.result.totalCost - right.result.totalCost || compareText(left.id, right.id);
}
var ranking = {
  maximizeScore: compareScore,
  maximizeWorst: (left, right) => right.result.worstDistrictScore - left.result.worstDistrictScore || compareScore(left, right),
  minimizeCost: (left, right) => left.result.totalCost - right.result.totalCost || compareScore(left, right)
};
function selectOptions(frontier, limit) {
  const leaders = new Map(OBJECTIVES.map((objective) => [objective, [...frontier].sort(ranking[objective])[0]]));
  const ordered = [.../* @__PURE__ */ new Set([
    ...leaders.values(),
    ...[...frontier].sort(compareScore)
  ])].filter(Boolean);
  return ordered.slice(0, limit).map((option) => ({
    ...option,
    selectedFor: OBJECTIVES.filter((objective) => leaders.get(objective) === option)
  })).map((option) => ({
    ...option,
    selectedFor: option.selectedFor.length ? option.selectedFor : ["tradeoff"]
  }));
}
var direction = (difference) => difference > 0 ? "improved" : difference < 0 ? "worse" : "unchanged";
function buildPolicyOptions(scenario, { limit = DEFAULT_LIMIT } = {}) {
  limit = Number.isFinite(limit) ? Math.max(0, Math.min(MAX_LIMIT, Math.floor(limit))) : DEFAULT_LIMIT;
  const baselineResult = simulate(scenario);
  const response = {
    valid: baselineResult.valid,
    scope: "single-decision-neighborhood",
    exhaustiveWithinScope: baselineResult.valid,
    baseline: {
      scenario: baselineResult.valid ? canonicalScenario(scenario.decisions) : structuredClone(scenario ?? null),
      result: baselineResult
    },
    explored: 0,
    validCandidates: 0,
    paretoCandidates: 0,
    limit,
    truncated: false,
    options: [],
    errors: baselineResult.errors,
    emptyReason: null
  };
  if (!baselineResult.valid) {
    response.emptyReason = {
      code: "INVALID_BASELINE",
      message: "Сначала исправьте исходный сценарий: альтернативы не рассчитывались."
    };
    return response;
  }
  const dataset = getDataset();
  const choices = dataset.measures.flatMap((measure) => measure.scope === "city" ? [{ measureId: measure.id }] : dataset.districts.map((district) => ({ measureId: measure.id, districtId: district.id })));
  const baselineScenario = response.baseline.scenario;
  const seen = /* @__PURE__ */ new Set([scenarioKey(baselineScenario)]);
  const candidates = [];
  for (const removed of baselineScenario.decisions) {
    const retained = baselineScenario.decisions.filter((decision) => decision !== removed);
    const retainedIds = new Set(retained.map((decision) => decision.measureId));
    for (const added of choices) {
      if (retainedIds.has(added.measureId)) continue;
      const candidateScenario = canonicalScenario([...retained, added]);
      const key = scenarioKey(candidateScenario);
      if (seen.has(key)) continue;
      seen.add(key);
      response.explored++;
      const result = simulate(candidateScenario);
      if (!result.valid) continue;
      response.validCandidates++;
      const delta = {
        score: result.score - baselineResult.score,
        worstDistrictScore: result.worstDistrictScore - baselineResult.worstDistrictScore,
        totalCost: result.totalCost - baselineResult.totalCost,
        criticalCount: result.criticalCount - baselineResult.criticalCount
      };
      candidates.push({
        id: `single:${key}`,
        scenario: candidateScenario,
        result,
        delta,
        changed: { removed: { ...removed }, added: { ...added } },
        objectives: {
          maximizeScore: direction(delta.score),
          maximizeWorst: direction(delta.worstDistrictScore),
          minimizeCost: direction(-delta.totalCost)
        }
      });
    }
  }
  const frontier = candidates.filter((candidate) => improvesAny(candidate.result, baselineResult) && !dominates(baselineResult, candidate.result) && !candidates.some((other) => dominates(other.result, candidate.result)));
  response.paretoCandidates = frontier.length;
  response.options = selectOptions(frontier, limit);
  response.truncated = frontier.length > response.options.length;
  if (!frontier.length) {
    response.emptyReason = {
      code: response.validCandidates ? "NO_IMPROVING_ALTERNATIVES" : "NO_VALID_ALTERNATIVES",
      message: response.validCandidates ? "Среди всех допустимых замен одного решения нет недоминируемой альтернативы, улучшающей хотя бы одну из трёх целей. Для иных вариантов потребуется изменить несколько решений." : "Ни одна замена одного решения не прошла официальную проверку ограничений."
    };
  } else if (limit === 0) {
    response.emptyReason = { code: "LIMIT_ZERO", message: "Альтернативы рассчитаны; вывод отключён параметром limit=0." };
  }
  return response;
}

// src/browser/policy-options-worker.js
self.addEventListener("message", ({ data }) => {
  const requestId = data?.requestId;
  if (typeof requestId !== "string" || !requestId || requestId.length > 128) return;
  try {
    const result = buildPolicyOptions(data.scenario, { limit: data.limit ?? 6 });
    self.postMessage({ requestId, data: result });
  } catch {
    self.postMessage({ requestId, error: {
      code: "POLICY_SEARCH_FAILED",
      message: "Не удалось найти альтернативы. Проверьте план и повторите поиск."
    } });
  }
});
