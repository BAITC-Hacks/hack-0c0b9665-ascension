# Проверка визуального слоя

23 сентября 2026, около 16:03 GMT+5. Визуальный исходный код: `28b9139` (включает графику `13b19e9` и motion `9cc46ae`). Проверка объединённого приложения выполнена на frontend `81afcff`, через локальный сервер `127.0.0.1:3027`.

Preview добавляет два stylesheet и module script, перемещает единственный `#district-focus` в начало `#app` перед метриками. Это согласованный контракт следующего подключения владельцем `index.html`; визуальная ветка не меняет общие HTML/JS приложения.

| Проверка | Наблюдаемый результат |
| --- | --- |
| Первый экран desktop 1280 | Графическая подложка hero, район/приоритеты/CTA, контрастный исходный Score. Все CSS загружены в правильном порядке. |
| Переполнение desktop | `scrollWidth=1265`, `innerWidth=1280`; ширина страницы соответствует clientWidth с полосой прокрутки. |
| Официальный пример через UI | 5/5 решений, стоимость 95, остаток 5; расчёт Score 56,54, прирост 3,99, критических показателей 0. |
| Альтернативы в браузере | 5 карточек, 181 проверенная замена / 117 допустимых. Первый вариант Score 57,21; визуальные стили не мешают Web Worker. |
| Библиотека | «Проверка нового дизайна» сохранена; счётчик 1/20, запись сохранилась после перезагрузки. Только локальный адрес preview. |
| Мобильная ширина 390×844 | clientWidth/scrollWidth 375/375, horizontal overflow отсутствует. Нижняя граница кнопки «Подобрать меры» 604,15 px, внутри первого экрана. |
| Другой город | После выбора Алматы видимых `.model-only` — 0; географическое уведомление показано. Возврат в Астану сохранил 5/5 мер и восстановил блок района. |
| Скрытые состояния | Элементов `[hidden]` с видимым computed display — 0. |
| Reduced motion | При `prefers-reduced-motion: reduce` `document.getAnimations().length=0`, scroll-behavior auto; после выбора демо движение также отсутствует. |
| Клавиатура | Tab с кнопки демо перешёл на reset-button; `:focus-visible=true`, outline solid. |
| Консоль | В общей проверке отсутствуют сообщения error/warn. |
| Статическая проверка | `node --check public/motion.js`, `git diff --check` — PASS. |

Внешний LLM в preview не подключён; проверено явно обозначенное расчётное пояснение. Это не повторная проверка AI или математического ядра. Прежние 144 теста browser integration относятся к его неизменённым исходникам; визуальная проверка дополняет, а не подменяет их.

Дополнительная проверка motion обнаружила удержание удалённых offscreen-элементов в IntersectionObserver; исправление включено. Независимое чтение подтвердило очистку. Автор motion также проверил 12 замен вне экрана (0 detached observed), прерывание при фокусе и работу UI без модуля. Подробности: [interface-motion-notes.md](interface-motion-notes.md).

SVG проверены как XML, без script/внешних ссылок и с размером каждого менее 30 KB. Внешние библиотеки, шрифты и raster assets не добавлены. Подробности: [city-graphics-notes.md](city-graphics-notes.md).

После merge необходимо подключить файлы по [контракту](visual-design.md), выполнить согласованный перенос блока района и проверить опубликованный адрес. На момент этой записи публикация визуального слоя ещё не выполнялась.
