# Проверка живого AI — 23 сентября 2026

В 17:23 GMT+5 публичный `/api/explain` успешно обработал два различных сценария: HTTP 200, `mode: ai`, `available: true`, модель `gpt-6-astra`. Ответы заняли 8,63 и 8,56 секунды. [Полные входы, ответы и контрольные результаты](evidence/ai-live-verification-2026-09-23.json).

Проверка относится к опубликованному исходнику `9b5a774506cd81094a4cb2af0bc3d2b0c7e0c9a3`, Cloudflare `3ade751b-70bb-4b04-a579-3246fdadc00d`. Код объяснения и его HTTP/runtime-обвязки совпадает с локальной базой аудита `9f3ec66c85d8aebcaed4200f1e93cb8e6c088082`. Это свидетельство конкретных вызовов, а не обещание постоянной доступности квоты.

## Что действительно проверено

| Показатель | A: школа в Нуре | B: школа в Есиле |
| --- | ---: | ---: |
| Стоимость | 95 | 95 |
| Score | 56,54307 | 55,29777 |
| Критических показателей | 0 | 1 |
| Школы и детсады Нуры, S1 | 48 | 38 |
| Школы и детсады Есиля, S1 | 48 | 58 |

Все остальные меры совпадают: поликлиника и освещение с камерами в Нуре, общегородская платформа обращений, чистое топливо в Сарыарке. Серверные расчёты проверены сравнением с локальным расчётным ядром.

Различие видно именно в тексте модели: для A она объяснила пользу школы и детсада для Нуры; для B перенесла эту пользу на Есиль и указала сохраняющийся критический дефицит школ и детсадов Нуры. Числовой префикс ответа и готовые формулировки рекомендаций добавляет сервер, поэтому их одних недостаточно для проверки анализа.

Отдельный сценарий с корректными идентификаторами и бюджетом 105 отклонён `/api/explain`: HTTP 422, единственная ошибка `BUDGET_EXCEEDED`. Провайдер вызывается только после проверки допустимости сценария.

## Роль и границы модели

- Расчёт Score, бюджета, эффектов и совместимости выполняет код. Языковая модель получает результаты и формулирует сильные стороны, риски и объяснение компромиссов. Обработчик действительно обращается к OpenAI Responses API; в публичном Worker кеш успешных объяснений этого модуля отключён.
- Вход модели — синтетический учебный сценарий. Живые обращения жителей, транспортные потоки и объекты карты в этот анализ не передаются. Два успешных примера не доказывают точность любого ответа и не превращают результат в прогноз для Астаны.
- Числа и допустимые рекомендации ограничены сервером. Свободный качественный текст не проходит полную проверку смысла и может содержать неточности. В проверенной версии объяснение также не получает полного справочника названий и весов всех кодов показателей; это ограничение контекста для дальнейшего улучшения.
- При ошибке, лимите или отсутствии ключа объяснение возвращает явно обозначенный расчётный режим: `mode: deterministic`, `available: false`. `/api/health` показывает только наличие конфигурации и не доказывает живой ответ.
- `/api/plan` — отдельная функция составления плана по тексту. Её первый живой прогон выявил ошибочную классификацию расчёта бюджета как неподдерживаемого запроса; сервер заблокировал применение. После исправления повтор 23 сентября в 17:43 GMT+5 на `241c84b` прошёл: пять исходных мер, `valid: true`, бюджет 95, успешное применение и девять кадров с итоговым Score 56,54307. [Отдельное свидетельство полного маршрута](evidence/ai-plan-final-verification.json). Результаты двух объяснений выше и проверка планирования относятся к отдельным вызовам и указанным версиям.

Автоматические проверки с подставными ответами провайдера подтверждают обработку контрактов и отказов, но не доступность внешнего сервиса. Повторные платные вызовы сверх двух объяснений для этого аудита не выполнялись.
