# Проверка сравнительной UI-ветки

23.09.2026, локальный Node 26.9.0, ветка `codex/ui-release-candidate`. Данные QA — отдельные SQLite/JSON в `/tmp`; AI выключен. Production, реальные обращения и Telegram этой проверкой не изменялись.

- `npm test`: **378/378 PASS**, включая поздние ответы при смене города/сбросе, новый demo/reset и цветные сектора карты.
- `node scripts/check-quality.js`: PASS, синтаксис, существование импортов, границы модулей и security headers.
- `npm run check:policy-worker`: PASS, сохранённая сборка соответствует общему ядру.
- `RELEASE_BASE_URL=http://127.0.0.1:3100 npm run check:release`: PASS — 8 страниц, 79 локальных ресурсов, 25 разных иллюстраций, 5 вкладок акима, 87 GET. Сверяются bytes и Content-Type, JSON API не подменяется HTML 200.
- `npx --yes wrangler@4.136.3 deploy --dry-run`: PASS, Worker с прежними AI bindings. Это не публикация и не подтверждение доступности API кабинетов в Worker.

Браузерная проверка объединённого UI через Codex browser: страницы `/`, `/citizens.html`, `/mayor.html`, `/desk.html`, `/akim.html`, `/resident.html`, `/results.html`, `/demo.html` открыты. На 320px горизонтального переполнения страницы и видимых ошибок нет. Для resident без приватного токена показывается предложение открыть персональную ссылку; пустые public results отображаются как пустое состояние.

Через отдельную локальную QA-учётную запись открыты все пять вкладок акима: сводка, проблемы, поручения, решения, совещание. Ошибок страницы и overflow нет. Учебная демонстрация пройдена по всем пяти шагам; поручение фиксируется только в памяти демо.

На 1440px просмотрены hero, четыре карточки показателей и пять районных карточек; все районные изображения загрузились, битых изображений нет. «Запустить демо» автоматически даёт Score **56,54**, 5 решений, стоимость **95** и остаток **5**, фокус переходит на результат. «Начать заново» возвращает 0 решений/100 бюджета и фокус конструктора. После сброса сохраняются интерактивные кнопки всех пяти районов; обнаруженный порядок перерисовки исправлен и закреплён регрессией. Расчёт, библиотека, записка и реестр действий подключены к общим событиям.

Обновлены селекторы сохранённых E2E для общей навигации и нового демо. Полные старые Playwright E2E в этой задаче повторно не запускались; перечисленные браузерные проверки выполнены отдельно. Проверки автора карты: 10/10 реальных браузерных сценариев в PR #34, включая 320/390/670/768/820px; это отдельное свидетельство, не наш повторный запуск.

На публичном домене этот сравнительный checkout не развёрнут. API кабинетов остаётся Node/SQLite; подключение к выбранному реестру PR #25 — задача согласованного адаптера. Основной владелец должен повторить проверку на фактически опубликованном SHA.
