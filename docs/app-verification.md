# Проверка приложения · 23 сентября 2026

> Исторический локальный прогон до подключения ключа и публикации карты. Раздел «Что остаётся» описывает состояние на момент этого прогона. Актуальные результаты публикации и живого AI — в [map-verification.md](map-verification.md).

Окружение: Windows, Node.js v24.19.0, локальный HTTP-сервер 127.0.0.1:3000. Проверяемая сборка включает сервер и AI-модуль `aba0ccc`, последующее ограничение reasoning для модели по умолчанию, базовый интерфейс и README из того же интеграционного PR.

## Автоматическая проверка

`npm test`: **63/63 PASS** — 25 проверок расчётного ядра, 27 объяснений, 11 HTTP-интеграций. После изменения ограничения reasoning отдельно повторены 27 тестов объяснений: PASS. `node --check public/app.js`: PASS.

HTTP-проверки используют реальные локальные сокеты: официальный сценарий 200/95/56.54307, перерасход 422 без Score, плохой JSON 400, неподдерживаемый тип 415, превышение 32 KiB 413. Проверены запреты доступа к env-файлам, обхода каталогов и symlink наружу, безопасные ошибки.

Ответы LLM в тестах **подставные**. Тесты проверяют факты запроса, JSON-схему, ошибки провайдера/сети/таймаута, отказ модели и честное запасное пояснение. Это не подтверждение живого AI.

## Проверка в браузере

- Новый сеанс: исходный Score 52,56, бюджет/остаток 100, выбор 0/5; все 14 мероприятий и 5 направлений.
- Кнопка «Загрузить демо»: пять официальных решений, стоимость 95, остаток 5. Расчёт с клавиатуры: Score 56,54, прирост 3,99, критических 0, синергия M10 + M12 в Нуре.
- Смена района M7 с Нуры на Есиль очищает предыдущий результат после серверной валидации. Новый расчёт: Score 55,30, прирост 2,74, критических 1; стоимость прежняя 95.
- Удаление M10 оставляет 4 решения стоимостью 83. Попытка добавить M2 даёт «Стоимость 105 превышает бюджет 100», выбор остаётся 4/5 и 83; итоговый расчёт недоступен.
- Без ключа виден статус «Без AI · расчётное объяснение», фактические цифры и проверенная допустимая замена. Кнопка повторного анализа доступна. Режим не обозначается как успешный AI-анализ.
- Консоль браузера: ошибок и предупреждений не обнаружено.
- Размер 390×844: горизонтального переполнения страницы нет; ширина содержимого 375 при viewport 390, таблица имеет отдельную область прокрутки. Панель выбора располагается перед каталогом. Размер браузера после проверки восстановлен.

## Что остаётся

OpenAI Platform вернул требование повторной авторизации. Ключ не создан; живой LLM-вызов и доступность модели ещё не проверены. Это обязательная оставшаяся часть кейса, а не завершённая AI-функция.

Личные сценарии Данияра H12-SC-01 интегрируются после его PR. Публичный хостинг, независимый компьютер жюри и финальная сдача на платформе не проверены этим прогоном.

## Интеграция панели сравнения

Личный коммит Алишера `83aa63f1` из PR #3 принят с сохранением истории. Подключены его CSS и ES module в общей странице. Изолированные CSS-правки интегратора предотвращают наследование широких таблиц основного интерфейса.

На Node v24.19.0 синтаксис comparison.js проверен. В полном приложении официальный расчёт сохранился как A (56,54); перенос M7 в Есиль — как B (55,30), разница районов: Есиль +1,10, Нура −1,10. «Загрузить A» восстановил Нуру в поле M7 и сбросил результат; следующий расчёт сдвинул прежний B в A и поместил 56,54 в B. Проверен полный путь HTTP → форма → событие → панель → загрузка в форму.

На ширине 390 px ширина документа 375, таблицы сравнения 309,6 (scrollWidth 310): горизонтального переполнения нет. Ошибок и предупреждений в консоли не обнаружено. Временные настройки размера восстановлены. Проверка исходного приложения из чистого архива `b0776a1` без установки зависимостей: HTML 200, официальный сценарий 200/95/56.54307.
